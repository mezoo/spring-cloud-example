spring:
  cloud:
    config:
      name: book-service
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/
